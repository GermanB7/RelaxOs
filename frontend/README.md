# TranquiloOS Frontend

React + Vite + TypeScript foundation for Sprint 0.

## Commands

```bash
npm install
npm run dev
npm run build
```

The app expects `VITE_API_BASE_URL=http://localhost:8080/api/v1` for local backend calls.
